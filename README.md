# Pivo MC

Этот репозиторий автоматически управляется через https://github.com/pxc1984/mcmanager. Папки `mods/`, `plugins/` и `configs/` автоматически
перекопируются в директорию с данными в майнкрафт сервере.
